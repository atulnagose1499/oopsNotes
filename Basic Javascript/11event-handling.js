// click listener - to check for any click 
button.addEventListener('click', function(event) {
    // track where users clicked the most
    hideModal();
});

// page onload listener - to do something on page load.
window.addEventListener('load', function(event) {
    // track user session time
});

// JavaScript Events
// Event	    Description
// onchange	    An HTML element has been changed
// onclick	    The user clicks an HTML element
// onmouseover	The user moves the mouse over an HTML element
// onmouseout	The user moves the mouse away from an HTML element
// onkeydown	The user pushes a keyboard key
// onload	    The browser has finished loading the page


// String Methods and Properties

// length : property returns the length of a string:
// Extracting String Parts
// slice(start, end) : extracted part in a new string.
// substring(start, end) : -ve index not allow
// substr(start, length) : second parameter specifies the length of the extracted part
